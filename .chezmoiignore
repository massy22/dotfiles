{{- /* このリポジトリ自身のファイル。$HOME に配置する対象ではない。
     とくに CLAUDE.md は ~/CLAUDE.md になると $HOME 以下の全プロジェクトの
     セッションに読み込まれてしまうため、必ず除外しておく。 */}}
CLAUDE.md
Makefile
README.md

{{ if ne .pcType "work" -}}
{{- /* OTel ヘッダは work 専用。テンプレートが 1Password を無条件に読むため、
     personal で管理対象に含めると apply が op signin で失敗する。 */}}
.claude/otel-headers-helper.sh
{{ end -}}
